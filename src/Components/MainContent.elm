module Components.MainContent exposing (view)

import Html exposing (Attribute, Html, h1, text)
import Html.Attributes exposing (class, style)
import Messages exposing (Msg)


view : Html Msg
view =
    text "Here goes main content"
